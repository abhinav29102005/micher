"""
Micher — Multi-link network bonding.

Combines multiple network interfaces (Ethernet + WiFi + hotspot) into a single
high-throughput transfer, so total speed approaches the sum of each link's
bandwidth instead of being capped at one.
"""

__version__ = "1.0.0"
__app_name__ = "Micher"
